

#Region "Events"
        
        (Es wird eine andere Lösung verwendet mit zusätzlicher Funktionalität)

        'Auch aus einem Modul heraus kann man Events feuern.
        'Sie müssen nur etwas anders verdrahtet werden.
        'Es geht hier kongret darum, das das UserControl UctlSpielfeld
        'angewiesen werden soll, sich neu zu zeichnen.
        'Hier ist das Event:
        Public Event RedrawRequested(aktVisibleUserControlValue As frmMain.VisibleUserControl)
        '
        'und der Aufruf dazu.
        Private Sub RequestRedraw()
            If _aktVisibleUserControlValue = frmMain.VisibleUserControl.Spielfeld OrElse
                    _aktVisibleUserControlValue = frmMain.VisibleUserControl.SpielfeldEditor Then

                RaiseEvent RedrawRequested(_aktVisibleUserControlValue)
            End If
        End Sub
        '
        ''Im UCtlSpielfeld muss dann stehen: 

        'Public Sub New() (Oder im Load-Ereignis)
        '    InitializeComponent()
        '    AddHandler SpielfeldDaten.RedrawRequested, AddressOf OnRedrawRequested
        'End Sub

        'Private Sub OnRedrawRequested()
        '    Me.Invalidate()
        'End Sub

#End Region


